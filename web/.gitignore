# dependências
/node_modules

# builds
/.next/
/build

# lockfile (o CI usa "npm install"; versões fixadas no package.json)
/package-lock.json

# ícones PNG gerados no build a partir de assets/*.svg (npm run prebuild)
/public/*.png

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ambiente
.env*.local

# typescript
next-env.d.ts
*.tsbuildinfo

# sistema
.DS_Store
