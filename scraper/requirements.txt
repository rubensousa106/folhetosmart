# FolhetoSmart — produtor de folhetos.
# Modelo "extrai 1x/semana no servidor, a app lê N x" via Cloudflare R2.
# Dependências do pipeline atual: drive_producer + normalize_products + rotate_r2 + aldi.

# --- Extração de produtos (PDF -> texto -> Claude) ---
anthropic>=0.40.0
pdfplumber>=0.11
pypdfium2>=4.30                   # render das páginas p/ o fallback de VISÃO (folhetos só-imagem)
Pillow>=10.0                     # converte o render em PNG para enviar ao Claude

# --- HTTP / scraping do folheto Aldi ---
requests>=2.31
beautifulsoup4>=4.12              # descoberta do link do PDF no site do Aldi
httpx>=0.27                       # envio das notificações FCM (HTTP v1)

# --- Cloudflare R2 (S3-compatível): folhetos PDF + feed JSON ---
boto3>=1.34

# --- Base de dados (só leitura dos tokens FCM em notifications/) ---
psycopg[binary]>=3.1

# --- Firebase Cloud Messaging: token de acesso da service account ---
google-auth>=2.30

# --- Redes com inspeção TLS (dev): confiar no cert store do SO ---
truststore>=0.9

# --- Testes / smoke do extrator ---
pytest>=8.0
